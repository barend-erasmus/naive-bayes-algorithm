export class Dataset {
    constructor (public key: string, public category: string) {

    }
}

export class Human {
    constructor (public gender: string, public height: number, public weight: number, public footSize: number) {

    }
}